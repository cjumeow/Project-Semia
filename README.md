# Project Semia

Master languages through seamless immersion and semantic understanding.

## Install

```bash
npm install
npm run build
```

In Chrome, open `chrome://extensions`, enable **Developer mode**, choose **Load unpacked**, and select `apps/extension/dist`.

