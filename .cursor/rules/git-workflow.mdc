---
description: Git 分支與 PR — 功能必須 PR，version bump 可在 main
alwaysApply: true
---

# Git Workflow

**在分支上做的功能，一定要先 PR merge 進 `main`。**

Version bump（`manifest.json` / `package.json` / `README.md`）**可以**直接在 `main` 上做，但 push / tag / release 仍需使用者明確允許 → `publish-guard.mdc`。

## 禁止（Agent 不要做）

- 在 `main` 上直接 `git commit` **功能代碼**
- 為了合併功能而 `git push origin main`（繞過 PR）
- 在 feature branch 做完功能後不開 PR 就 push 到 `main`
- 未經允許自行 `gh pr create` 或發版（見 `publish-guard.mdc`）

v0.5.1 功能 commit 直接在 `main` 上開發、沒有 PR，是錯誤範例。

## 功能開發（必須 PR）

1. 從 `main` 開 feature branch（例：`feat/v0.5.2`）
2. 在 branch 上開發、commit、跑 `npm run verify`
3. push branch（若需要），等使用者明確允許後再 **開 PR**
4. **PR merge 進 `main`**（由使用者或 CI merge，不是 Agent 為功能直接 push main）

## Version bump（可在 main）

功能 PR merge 後，發版時可在 `main` 上 bump；**push main / tag / release 仍須使用者說可以**：

```bash
git checkout main && git pull
# 更新 manifest / package.json / README
git commit -m "chore(extension): bump version to X.Y.Z"
# 停在此，詢問是否 push / tag
```

也可把 bump 放在功能 PR 裡一起 review。

## 與其他規則

- 開 PR / 發版許可 → `publish-guard.mdc`
- Tag、README、CI Release → `release-workflow.mdc`
