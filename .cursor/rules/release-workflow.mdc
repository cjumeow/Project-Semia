---
description: 發版流程 — version bump、README、build、tag、GitHub Release（Agent 直接執行）
alwaysApply: true
---

# Release Workflow

使用者說「發版」「release」「bump version」「出新版」時，**由 Agent 在終端完成全流程**，不要只列步驟讓使用者自己做。

## 版本號

- 讀取目前 `version_name`（以 `apps/extension/manifest.json` 為準），遞增：
  - **patch**（0.4.0 → 0.4.1）：小修、CI、docs
  - **minor**（0.4.0 → 0.5.0）：新功能、跨檔改動
- 同步更新這四處為同一版本號：
  - `apps/extension/manifest.json` → `version` 與 `version_name`
  - `apps/extension/package.json` → `version` 與 `version_name`

## README

在 `README.md` Releases 表格**最上方**新增一行：

| 欄位 | 格式 |
|------|------|
| Version | `[vX.Y.Z](https://github.com/cjumeow/Project-Semia/releases/tag/vX.Y.Z)` |
| Date | 當天 `YYYY-MM-DD` |
| Summary | 一句話（英文或中文皆可，精簡） |

並更新 Install 行：`semia-extension-vX.Y.Z.zip`。

## Build 與打包

```bash
npm run build
cd apps/extension && zip -r ../../semia-extension-vX.Y.Z.zip manifest.json options.html dist/
```

- zip **不要** commit 進 git（除非使用者明確要求）
- zip 根目錄須含 `manifest.json`、`options.html`、`dist/`

## Git（在 `main` 上）

1. 確認在 `main` 且與 `origin/main` 同步（必要時 pull/rebase）
2. 單獨 commit version bump：

```bash
git add apps/extension/manifest.json apps/extension/package.json README.md
git commit -m "chore(extension): bump version to X.Y.Z"
git push origin main
```

3. Tag 與 Release：

```bash
git tag vX.Y.Z
git push origin vX.Y.Z
gh release create vX.Y.Z semia-extension-vX.Y.Z.zip \
  --title "vX.Y.Z" \
  --notes "$(cat <<'EOF'
<精簡 release notes，見下方格式>
EOF
)"
```

## Release notes 風格（給自己看）

- **3～5 條 bullet**，每條一行，不寫長段落
- 只列「這版多了什麼 / 修了什麼」，不寫教學、不寫致謝、不寫 migration 長文
- 範例：

```markdown
- SEMIA library UI + capture sync
- AI snippet notes (DeepSeek / OpenAI, extension Options)
- YouTube thumbnail preview instead of embed
```

## 完成後

- 回報：版本號、commit、tag、Release URL
- 提醒：`chrome://extensions` reload + 核心 smoke test

## 與其他規則

- 發版屬於 **直接 push `main`**，不開 PR
- 使用者若只說「準備發版內容」但未說 push，先完成 bump + build + zip，**詢問是否 push/tag/release**
