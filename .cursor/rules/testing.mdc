---
description: 測試原則 — 測什麼、不測什麼、何時寫、如何驗證
alwaysApply: true
---

# Testing

測試的目的是**在使用者手動測試發現不了的地方發出警報**，不是追覆蓋率。
工具：Vitest（root `vitest.config.ts`），測試檔與原始碼同目錄，命名 `*.test.ts`。

## 測 / 不測

| 測 | 為什麼 |
|---|---|
| 純函式（輸入→輸出，無副作用） | 便宜、穩定、壞了不易被眼睛看到 |
| 解析器（XML / transcript / URL） | 外部輸入格式多變，最容易靜默壞掉 |
| 分組、排序、時間換算 | 藏著判斷邏輯 |
| 向下相容（舊 storage 資料缺新欄位） | 保護使用者既有資料 |
| 刻意選定的常數（如 30s context window ±15s） | 防止未來被無意改掉 |

| 不測 | 為什麼 |
|---|---|
| React 元件渲染、樣式、class name | 脆弱、手動看畫面更快 |
| `chrome.storage` / `chrome.runtime` 整合 | 要假造整個 Chrome API，回報低 |
| AI 產出的**內容** | 不確定；只測「解析 AI 回覆」那一段 |
| 型別檔、常數檔、mock 資料 | 沒有行為 |
| 只用一次的 3～5 行 helper | 讀 code 比讀測試快 |

## 何時寫（不要累積成待辦）

1. **新增純函式** → 測試寫在**同一個 PR**。此時成本幾乎為零；事後補要重新猜當初意圖，且容易寫成「照現行行為抄一遍」的無效測試。
2. **修 bug** → 先寫一個**會失敗**的測試，再修到它通過。手動測出的每個 bug 都證明那裡真的會壞。
3. **改既有純函式行為** → 先更新測試表達新意圖，再改實作。
4. **只改 UI / 樣式 / 文案** → 不需要新測試。

## 寫法

- 測**不變量**，不測偶然實作細節。
  例：`tokenizeCue` 斷言「所有 token 接回來等於原字串」，而非斷言 `Intl.Segmenter` 切出幾段。
- 一個 `it` 一個行為，名稱寫「應該怎樣」而非函式名。
- fixture 就近建在測試檔內；**不要**為了共用 fixture 讓 extension ↔ corpus 互相 import（見 `architecture.mdc`）。
- 需要模擬舊資料時可用 `as SnippetNote` 之類的窄化轉型，並在測試名稱說明是舊資料情境。

## 交付時必須回報

加了測試就要說明：

1. 測了哪些**行為**（不是列檔名）
2. **實際跑過**的結果（`npm test` 輸出）
3. 對關鍵測試做過**反向驗證**——故意改壞實作、確認測試會失敗、再還原

第 3 點不可省。沒有反向驗證的測試無法證明它會叫。

## 指令

```bash
npm test          # 跑一次
npm run test:watch # 開發中持續跑
npm run verify    # typecheck → test → build（發版前 / 推 PR 前）
```

CI 在 PR 與 push `main` 時自動跑 `verify` 前三關（見 `.github/workflows/ci.yml`）。

## 與其他規則

- 模組邊界、該不該抽介面 → `architecture.mdc`
- 發版前的 verify 與版本檢查 → `release-workflow.mdc`
