---
description: 架構原則 — 模組分離、低耦合、避免過度工程
alwaysApply: true
---

# Architecture

寫 code 時同時維護邊界：**小步交付、介面清楚、依賴單向**。不為假想需求加層，但新功能要放在對的模組。

## Monorepo 邊界

| 區域 | 職責 | 不應做 |
|------|------|--------|
| `packages/shared` | 跨 app 型別、storage keys、純資料契約 | UI、Chrome API、AI、React |
| `apps/extension` | YouTube 擷取、background、storage 寫入、AI 呼叫 | React、SEMIA 版面 |
| `apps/corpus` | SEMIA UI、讀取/觸發 extension 能力 | 直接碰 YouTube DOM、繞過 repository |
| `scripts/` | build 組裝（corpus → extension dist） | 產品邏輯 |

**依賴方向：** `corpus` / `extension` → `shared`；**禁止** `shared` 依賴 app；**禁止** extension ↔ corpus 直接 import。

## Extension 內部分層

```
contentScript / sidebarPanel   →  UI 與使用者操作（YouTube 頁）
background.ts                  →  message 路由、編排（薄）
*Storage.ts                    →  chrome.storage 讀寫
ai/                            →  prompt、parse、API 呼叫（無 DOM）
types.ts                       →  BackgroundMessage、extension-only 型別
```

- **新 Chrome message**：型別加 `types.ts` + `background` handler；corpus 透過 `corpusRepository` 呼叫，不在元件裡散落 `sendMessage` 字串。
- **新 AI 能力**：`ai/build*Prompt.ts` + `ai/generate*.ts` + `ai/parse*.ts`；共用 `chatCompletion.ts`，不重複 fetch 邏輯。
- **content script 保持薄**：擷取與 UI 事件；持久化與 AI 交 background。

## Corpus 內部分層

```
components/     →  展示與互動（盡量無資料來源細節）
hooks/          →  狀態、訂閱、呼叫 repository
data/           →  corpusRepository（Chrome / mock 實作）
utils/          →  純函式轉換（fragmentToSnippet、grouping）
types/          →  UI 專用型別；domain 型別用 @semia/shared
```

- UI **不**直接 `chrome.storage` / `chrome.runtime`（除 `corpusRepository` 實作內）。
- 新資料來源：擴充 `CorpusRepository` 介面 + 實作，不讓元件知道 background message 名稱。

## 介面與耦合

**要抽介面的情況：**
- 跨邊界（corpus ↔ extension、storage ↔ UI）
- 同一邏輯有第二個實作或明顯會測試 mock（如 `MockCorpusRepository`）
- 型別會被兩個 app 共用 → 放 `packages/shared`

**不必抽的情況：**
- 只用一次的 3～5 行 helper
- 為「將來可能」建的抽象層、factory、event bus
- 把單一呼叫包成 class 卻沒有第二種行為

**型別單一來源：**
- Domain（`LanguageFragment`、`SnippetNote`）→ `@semia/shared`
- 不在 extension / corpus 各複製一份；改欄位先改 shared，再改 parser / UI。

## 改動時自問（Agent）

1. 這段邏輯屬於哪個邊界？放錯資料夾就搬。
2. 能否用**現有**函式/介面延伸，而非新架構？
3. 耦合是否集中在**一個模組**（repository、background handler），而非散在多個元件？
4. diff 是否只涵蓋任務範圍？不順手重構無關檔案。
5. 新檔案是否必要？能併入相鄰模組且仍清晰則不開新檔。

## 反模式（避免）

- Corpus 元件內寫 `chrome.runtime.sendMessage({ type: '...' })`
- Extension import corpus 或 React
- 在 `shared` 放 Chrome / React / prompt 字串
- 一個 300 行元件同時處理 UI、API、storage
- 為單一 PR 引入 DI 框架、全域 store、通用 plugin 系統

## 與其他規則

- 小改範圍、build 驗證 → `project-overview.mdc`
- 分支 / PR / 發版 → `git-workflow.mdc`、`release-workflow.mdc`
